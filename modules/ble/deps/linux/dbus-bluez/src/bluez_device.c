/*
 * Generated by gdbus-codegen 2.40.0. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "bluez_device.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Introspectable
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:bluezdeviceOrgFreedesktopDBusIntrospectable
 * @title: bluezdeviceOrgFreedesktopDBusIntrospectable
 * @short_description: Generated C code for the org.freedesktop.DBus.Introspectable D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Introspectable ---- */

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_xml =
{
  {
    -1,
    (gchar *) "xml",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_xml,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_device_org_freedesktop_dbus_introspectable_method_info_introspect =
{
  {
    -1,
    (gchar *) "Introspect",
    NULL,
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_pointers,
    NULL
  },
  "handle-introspect",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _bluez_device_org_freedesktop_dbus_introspectable_method_info_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_introspectable_method_info_introspect,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_device_org_freedesktop_dbus_introspectable_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.DBus.Introspectable",
    (GDBusMethodInfo **) &_bluez_device_org_freedesktop_dbus_introspectable_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "org-freedesktop-dbus-introspectable",
};


/**
 * bluez_device_org_freedesktop_dbus_introspectable_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_device_org_freedesktop_dbus_introspectable_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct;
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #bluezdeviceOrgFreedesktopDBusIntrospectable interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_device_org_freedesktop_dbus_introspectable_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * bluezdeviceOrgFreedesktopDBusIntrospectable:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 */

/**
 * bluezdeviceOrgFreedesktopDBusIntrospectableIface:
 * @parent_iface: The parent interface.
 * @handle_introspect: Handler for the #bluezdeviceOrgFreedesktopDBusIntrospectable::handle-introspect signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 */

typedef bluezdeviceOrgFreedesktopDBusIntrospectableIface bluezdeviceOrgFreedesktopDBusIntrospectableInterface;
G_DEFINE_INTERFACE (bluezdeviceOrgFreedesktopDBusIntrospectable, bluez_device_org_freedesktop_dbus_introspectable, G_TYPE_OBJECT);

static void
bluez_device_org_freedesktop_dbus_introspectable_default_init (bluezdeviceOrgFreedesktopDBusIntrospectableIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * bluezdeviceOrgFreedesktopDBusIntrospectable::handle-introspect:
   * @object: A #bluezdeviceOrgFreedesktopDBusIntrospectable.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device_org_freedesktop_dbus_introspectable_complete_introspect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-introspect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceOrgFreedesktopDBusIntrospectableIface, handle_introspect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_call_introspect:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusIntrospectableProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_introspectable_call_introspect_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_introspectable_call_introspect_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device_org_freedesktop_dbus_introspectable_call_introspect (
    bluezdeviceOrgFreedesktopDBusIntrospectable *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Introspect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_call_introspect_finish:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusIntrospectableProxy.
 * @out_xml: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_introspectable_call_introspect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_introspectable_call_introspect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_introspectable_call_introspect_finish (
    bluezdeviceOrgFreedesktopDBusIntrospectable *proxy,
    gchar **out_xml,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_xml);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_call_introspect_sync:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusIntrospectableProxy.
 * @out_xml: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_introspectable_call_introspect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_introspectable_call_introspect_sync (
    bluezdeviceOrgFreedesktopDBusIntrospectable *proxy,
    gchar **out_xml,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Introspect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_xml);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_complete_introspect:
 * @object: A #bluezdeviceOrgFreedesktopDBusIntrospectable.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @xml: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device_org_freedesktop_dbus_introspectable_complete_introspect (
    bluezdeviceOrgFreedesktopDBusIntrospectable *object,
    GDBusMethodInvocation *invocation,
    const gchar *xml)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   xml));
}

/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceOrgFreedesktopDBusIntrospectableProxy:
 *
 * The #bluezdeviceOrgFreedesktopDBusIntrospectableProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceOrgFreedesktopDBusIntrospectableProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceOrgFreedesktopDBusIntrospectableProxy.
 */

struct _bluezdeviceOrgFreedesktopDBusIntrospectableProxyPrivate
{
  GData *qdata;
};

static void bluez_device_org_freedesktop_dbus_introspectable_proxy_iface_init (bluezdeviceOrgFreedesktopDBusIntrospectableIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusIntrospectableProxy, bluez_device_org_freedesktop_dbus_introspectable_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (bluezdeviceOrgFreedesktopDBusIntrospectableProxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, bluez_device_org_freedesktop_dbus_introspectable_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusIntrospectableProxy, bluez_device_org_freedesktop_dbus_introspectable_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, bluez_device_org_freedesktop_dbus_introspectable_proxy_iface_init));

#endif
static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_finalize (GObject *object)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableProxy *proxy = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_device_org_freedesktop_dbus_introspectable_proxy_parent_class)->finalize (object);
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableProxy *proxy = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_init (bluezdeviceOrgFreedesktopDBusIntrospectableProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_device_org_freedesktop_dbus_introspectable_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, bluezdeviceOrgFreedesktopDBusIntrospectableProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_device_org_freedesktop_dbus_introspectable_interface_info ());
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_class_init (bluezdeviceOrgFreedesktopDBusIntrospectableProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_device_org_freedesktop_dbus_introspectable_proxy_finalize;
  gobject_class->get_property = bluez_device_org_freedesktop_dbus_introspectable_proxy_get_property;
  gobject_class->set_property = bluez_device_org_freedesktop_dbus_introspectable_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_device_org_freedesktop_dbus_introspectable_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_device_org_freedesktop_dbus_introspectable_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceOrgFreedesktopDBusIntrospectableProxyPrivate));
#endif
}

static void
bluez_device_org_freedesktop_dbus_introspectable_proxy_iface_init (bluezdeviceOrgFreedesktopDBusIntrospectableIface *iface)
{
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_introspectable_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_introspectable_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device_org_freedesktop_dbus_introspectable_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_introspectable_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_introspectable_proxy_new().
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusIntrospectable *
bluez_device_org_freedesktop_dbus_introspectable_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_introspectable_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusIntrospectable *
bluez_device_org_freedesktop_dbus_introspectable_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}


/**
 * bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_device_org_freedesktop_dbus_introspectable_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusIntrospectable *
bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_device_org_freedesktop_dbus_introspectable_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusIntrospectable *
bluez_device_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton:
 *
 * The #bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceOrgFreedesktopDBusIntrospectableSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton.
 */

struct _bluezdeviceOrgFreedesktopDBusIntrospectableSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_device_org_freedesktop_dbus_introspectable_skeleton_vtable =
{
  _bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_method_call,
  _bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_get_property,
  _bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_device_org_freedesktop_dbus_introspectable_interface_info ();
}

static GDBusInterfaceVTable *
bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_device_org_freedesktop_dbus_introspectable_skeleton_vtable;
}

static GVariant *
bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_device_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_device_org_freedesktop_dbus_introspectable_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Introspectable", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void bluez_device_org_freedesktop_dbus_introspectable_skeleton_iface_init (bluezdeviceOrgFreedesktopDBusIntrospectableIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton, bluez_device_org_freedesktop_dbus_introspectable_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, bluez_device_org_freedesktop_dbus_introspectable_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton, bluez_device_org_freedesktop_dbus_introspectable_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, bluez_device_org_freedesktop_dbus_introspectable_skeleton_iface_init));

#endif
static void
bluez_device_org_freedesktop_dbus_introspectable_skeleton_finalize (GObject *object)
{
  bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_device_org_freedesktop_dbus_introspectable_skeleton_parent_class)->finalize (object);
}

static void
bluez_device_org_freedesktop_dbus_introspectable_skeleton_init (bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_device_org_freedesktop_dbus_introspectable_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON, bluezdeviceOrgFreedesktopDBusIntrospectableSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
bluez_device_org_freedesktop_dbus_introspectable_skeleton_class_init (bluezdeviceOrgFreedesktopDBusIntrospectableSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_device_org_freedesktop_dbus_introspectable_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_device_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceOrgFreedesktopDBusIntrospectableSkeletonPrivate));
#endif
}

static void
bluez_device_org_freedesktop_dbus_introspectable_skeleton_iface_init (bluezdeviceOrgFreedesktopDBusIntrospectableIface *iface)
{
}

/**
 * bluez_device_org_freedesktop_dbus_introspectable_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusIntrospectableSkeleton): The skeleton object.
 */
bluezdeviceOrgFreedesktopDBusIntrospectable *
bluez_device_org_freedesktop_dbus_introspectable_skeleton_new (void)
{
  return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (g_object_new (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.Device1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:bluezdevice
 * @title: bluezdevice
 * @short_description: Generated C code for the org.bluez.Device1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.Device1 ---- */

static const _ExtendedGDBusMethodInfo _bluez_device__method_info_disconnect =
{
  {
    -1,
    (gchar *) "Disconnect",
    NULL,
    NULL,
    NULL
  },
  "handle-disconnect",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_device__method_info_connect =
{
  {
    -1,
    (gchar *) "Connect",
    NULL,
    NULL,
    NULL
  },
  "handle-connect",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device__method_info_connect_profile_IN_ARG_UUID =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device__method_info_connect_profile_IN_ARG_pointers[] =
{
  &_bluez_device__method_info_connect_profile_IN_ARG_UUID,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_device__method_info_connect_profile =
{
  {
    -1,
    (gchar *) "ConnectProfile",
    (GDBusArgInfo **) &_bluez_device__method_info_connect_profile_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-connect-profile",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device__method_info_disconnect_profile_IN_ARG_UUID =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device__method_info_disconnect_profile_IN_ARG_pointers[] =
{
  &_bluez_device__method_info_disconnect_profile_IN_ARG_UUID,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_device__method_info_disconnect_profile =
{
  {
    -1,
    (gchar *) "DisconnectProfile",
    (GDBusArgInfo **) &_bluez_device__method_info_disconnect_profile_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-disconnect-profile",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_device__method_info_pair =
{
  {
    -1,
    (gchar *) "Pair",
    NULL,
    NULL,
    NULL
  },
  "handle-pair",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_device__method_info_cancel_pairing =
{
  {
    -1,
    (gchar *) "CancelPairing",
    NULL,
    NULL,
    NULL
  },
  "handle-cancel-pairing",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _bluez_device__method_info_pointers[] =
{
  &_bluez_device__method_info_disconnect,
  &_bluez_device__method_info_connect,
  &_bluez_device__method_info_connect_profile,
  &_bluez_device__method_info_disconnect_profile,
  &_bluez_device__method_info_pair,
  &_bluez_device__method_info_cancel_pairing,
  NULL
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_address =
{
  {
    -1,
    (gchar *) "Address",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "address",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_name =
{
  {
    -1,
    (gchar *) "Name",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "name",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_alias =
{
  {
    -1,
    (gchar *) "Alias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "alias",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_class =
{
  {
    -1,
    (gchar *) "Class",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "class",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_appearance =
{
  {
    -1,
    (gchar *) "Appearance",
    (gchar *) "q",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "appearance",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_icon =
{
  {
    -1,
    (gchar *) "Icon",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "icon",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_paired =
{
  {
    -1,
    (gchar *) "Paired",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "paired",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_trusted =
{
  {
    -1,
    (gchar *) "Trusted",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "trusted",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_blocked =
{
  {
    -1,
    (gchar *) "Blocked",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "blocked",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_legacy_pairing =
{
  {
    -1,
    (gchar *) "LegacyPairing",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "legacy-pairing",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_rssi =
{
  {
    -1,
    (gchar *) "RSSI",
    (gchar *) "n",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "rssi",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_connected =
{
  {
    -1,
    (gchar *) "Connected",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "connected",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_uuids =
{
  {
    -1,
    (gchar *) "UUIDs",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuids",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_modalias =
{
  {
    -1,
    (gchar *) "Modalias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "modalias",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_adapter =
{
  {
    -1,
    (gchar *) "Adapter",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "adapter",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_manufacturer_data =
{
  {
    -1,
    (gchar *) "ManufacturerData",
    (gchar *) "a{qv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "manufacturer-data",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_service_data =
{
  {
    -1,
    (gchar *) "ServiceData",
    (gchar *) "a{sv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "service-data",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_tx_power =
{
  {
    -1,
    (gchar *) "TxPower",
    (gchar *) "n",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "tx-power",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device__property_info_gatt_services =
{
  {
    -1,
    (gchar *) "GattServices",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "gatt-services",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _bluez_device__property_info_pointers[] =
{
  &_bluez_device__property_info_address,
  &_bluez_device__property_info_name,
  &_bluez_device__property_info_alias,
  &_bluez_device__property_info_class,
  &_bluez_device__property_info_appearance,
  &_bluez_device__property_info_icon,
  &_bluez_device__property_info_paired,
  &_bluez_device__property_info_trusted,
  &_bluez_device__property_info_blocked,
  &_bluez_device__property_info_legacy_pairing,
  &_bluez_device__property_info_rssi,
  &_bluez_device__property_info_connected,
  &_bluez_device__property_info_uuids,
  &_bluez_device__property_info_modalias,
  &_bluez_device__property_info_adapter,
  &_bluez_device__property_info_manufacturer_data,
  &_bluez_device__property_info_service_data,
  &_bluez_device__property_info_tx_power,
  &_bluez_device__property_info_gatt_services,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_device__interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.Device1",
    (GDBusMethodInfo **) &_bluez_device__method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_bluez_device__property_info_pointers,
    NULL
  },
  "",
};


/**
 * bluez_device__interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_device__interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_device__interface_info.parent_struct;
}

/**
 * bluez_device__override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #bluezdevice interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_device__override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "address");
  g_object_class_override_property (klass, property_id_begin++, "name");
  g_object_class_override_property (klass, property_id_begin++, "alias");
  g_object_class_override_property (klass, property_id_begin++, "class");
  g_object_class_override_property (klass, property_id_begin++, "appearance");
  g_object_class_override_property (klass, property_id_begin++, "icon");
  g_object_class_override_property (klass, property_id_begin++, "paired");
  g_object_class_override_property (klass, property_id_begin++, "trusted");
  g_object_class_override_property (klass, property_id_begin++, "blocked");
  g_object_class_override_property (klass, property_id_begin++, "legacy-pairing");
  g_object_class_override_property (klass, property_id_begin++, "rssi");
  g_object_class_override_property (klass, property_id_begin++, "connected");
  g_object_class_override_property (klass, property_id_begin++, "uuids");
  g_object_class_override_property (klass, property_id_begin++, "modalias");
  g_object_class_override_property (klass, property_id_begin++, "adapter");
  g_object_class_override_property (klass, property_id_begin++, "manufacturer-data");
  g_object_class_override_property (klass, property_id_begin++, "service-data");
  g_object_class_override_property (klass, property_id_begin++, "tx-power");
  g_object_class_override_property (klass, property_id_begin++, "gatt-services");
  return property_id_begin - 1;
}



/**
 * bluezdevice:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 */

/**
 * bluezdeviceIface:
 * @parent_iface: The parent interface.
 * @handle_cancel_pairing: Handler for the #bluezdevice::handle-cancel-pairing signal.
 * @handle_connect: Handler for the #bluezdevice::handle-connect signal.
 * @handle_connect_profile: Handler for the #bluezdevice::handle-connect-profile signal.
 * @handle_disconnect: Handler for the #bluezdevice::handle-disconnect signal.
 * @handle_disconnect_profile: Handler for the #bluezdevice::handle-disconnect-profile signal.
 * @handle_pair: Handler for the #bluezdevice::handle-pair signal.
 * @get_adapter: Getter for the #bluezdevice:adapter property.
 * @get_address: Getter for the #bluezdevice:address property.
 * @get_alias: Getter for the #bluezdevice:alias property.
 * @get_appearance: Getter for the #bluezdevice:appearance property.
 * @get_blocked: Getter for the #bluezdevice:blocked property.
 * @get_class: Getter for the #bluezdevice:class property.
 * @get_connected: Getter for the #bluezdevice:connected property.
 * @get_gatt_services: Getter for the #bluezdevice:gatt-services property.
 * @get_icon: Getter for the #bluezdevice:icon property.
 * @get_legacy_pairing: Getter for the #bluezdevice:legacy-pairing property.
 * @get_manufacturer_data: Getter for the #bluezdevice:manufacturer-data property.
 * @get_modalias: Getter for the #bluezdevice:modalias property.
 * @get_name: Getter for the #bluezdevice:name property.
 * @get_paired: Getter for the #bluezdevice:paired property.
 * @get_rssi: Getter for the #bluezdevice:rssi property.
 * @get_service_data: Getter for the #bluezdevice:service-data property.
 * @get_trusted: Getter for the #bluezdevice:trusted property.
 * @get_tx_power: Getter for the #bluezdevice:tx-power property.
 * @get_uuids: Getter for the #bluezdevice:uuids property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 */

typedef bluezdeviceIface bluezdeviceInterface;
G_DEFINE_INTERFACE (bluezdevice, bluez_device_, G_TYPE_OBJECT);

static void
bluez_device__default_init (bluezdeviceIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * bluezdevice::handle-disconnect:
   * @object: A #bluezdevice.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device__complete_disconnect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disconnect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceIface, handle_disconnect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * bluezdevice::handle-connect:
   * @object: A #bluezdevice.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device__complete_connect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceIface, handle_connect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * bluezdevice::handle-connect-profile:
   * @object: A #bluezdevice.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_UUID: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device__complete_connect_profile() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect-profile",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceIface, handle_connect_profile),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * bluezdevice::handle-disconnect-profile:
   * @object: A #bluezdevice.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_UUID: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device__complete_disconnect_profile() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disconnect-profile",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceIface, handle_disconnect_profile),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * bluezdevice::handle-pair:
   * @object: A #bluezdevice.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device__complete_pair() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pair",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceIface, handle_pair),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * bluezdevice::handle-cancel-pairing:
   * @object: A #bluezdevice.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device__complete_cancel_pairing() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-cancel-pairing",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceIface, handle_cancel_pairing),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * bluezdevice:address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("address", "Address", "Address", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("name", "Name", "Name", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:alias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("alias", "Alias", "Alias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:class:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Class">"Class"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("class", "Class", "Class", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:appearance:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Appearance">"Appearance"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("appearance", "Appearance", "Appearance", 0, G_MAXUINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:icon:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("icon", "Icon", "Icon", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:paired:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Paired">"Paired"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("paired", "Paired", "Paired", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:trusted:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Trusted">"Trusted"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("trusted", "Trusted", "Trusted", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:blocked:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Blocked">"Blocked"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("blocked", "Blocked", "Blocked", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:legacy-pairing:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.LegacyPairing">"LegacyPairing"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("legacy-pairing", "LegacyPairing", "LegacyPairing", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:rssi:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.RSSI">"RSSI"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("rssi", "RSSI", "RSSI", G_MININT16, G_MAXINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:connected:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("connected", "Connected", "Connected", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:uuids:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("uuids", "UUIDs", "UUIDs", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:modalias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("modalias", "Modalias", "Modalias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:adapter:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("adapter", "Adapter", "Adapter", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:manufacturer-data:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.ManufacturerData">"ManufacturerData"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("manufacturer-data", "ManufacturerData", "ManufacturerData", G_VARIANT_TYPE ("a{qv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:service-data:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("service-data", "ServiceData", "ServiceData", G_VARIANT_TYPE ("a{sv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:tx-power:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.TxPower">"TxPower"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("tx-power", "TxPower", "TxPower", G_MININT16, G_MAXINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdevice:gatt-services:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("gatt-services", "GattServices", "GattServices", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_device__get_address: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_address() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device__get_address (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_address (object);
}

/**
 * bluez_device__dup_address: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device__dup_address (bluezdevice *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "address", &value, NULL);
  return value;
}

/**
 * bluez_device__set_address: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_address (bluezdevice *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "address", value, NULL);
}

/**
 * bluez_device__get_name: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_name() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device__get_name (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_name (object);
}

/**
 * bluez_device__dup_name: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device__dup_name (bluezdevice *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "name", &value, NULL);
  return value;
}

/**
 * bluez_device__set_name: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Name">"Name"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_name (bluezdevice *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "name", value, NULL);
}

/**
 * bluez_device__get_alias: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_alias() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device__get_alias (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_alias (object);
}

/**
 * bluez_device__dup_alias: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device__dup_alias (bluezdevice *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "alias", &value, NULL);
  return value;
}

/**
 * bluez_device__set_alias: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Alias">"Alias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
bluez_device__set_alias (bluezdevice *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "alias", value, NULL);
}

/**
 * bluez_device__get_class: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Class">"Class"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
bluez_device__get_class (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_class (object);
}

/**
 * bluez_device__set_class: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Class">"Class"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_class (bluezdevice *object, guint value)
{
  g_object_set (G_OBJECT (object), "class", value, NULL);
}

/**
 * bluez_device__get_appearance: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Appearance">"Appearance"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint16 
bluez_device__get_appearance (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_appearance (object);
}

/**
 * bluez_device__set_appearance: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Appearance">"Appearance"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_appearance (bluezdevice *object, guint16 value)
{
  g_object_set (G_OBJECT (object), "appearance", value, NULL);
}

/**
 * bluez_device__get_icon: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_icon() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device__get_icon (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_icon (object);
}

/**
 * bluez_device__dup_icon: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device__dup_icon (bluezdevice *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "icon", &value, NULL);
  return value;
}

/**
 * bluez_device__set_icon: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Icon">"Icon"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_icon (bluezdevice *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "icon", value, NULL);
}

/**
 * bluez_device__get_paired: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Paired">"Paired"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device__get_paired (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_paired (object);
}

/**
 * bluez_device__set_paired: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Paired">"Paired"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_paired (bluezdevice *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "paired", value, NULL);
}

/**
 * bluez_device__get_trusted: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Trusted">"Trusted"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device__get_trusted (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_trusted (object);
}

/**
 * bluez_device__set_trusted: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Trusted">"Trusted"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
bluez_device__set_trusted (bluezdevice *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "trusted", value, NULL);
}

/**
 * bluez_device__get_blocked: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Blocked">"Blocked"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device__get_blocked (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_blocked (object);
}

/**
 * bluez_device__set_blocked: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Blocked">"Blocked"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
bluez_device__set_blocked (bluezdevice *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "blocked", value, NULL);
}

/**
 * bluez_device__get_legacy_pairing: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.LegacyPairing">"LegacyPairing"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device__get_legacy_pairing (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_legacy_pairing (object);
}

/**
 * bluez_device__set_legacy_pairing: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.LegacyPairing">"LegacyPairing"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_legacy_pairing (bluezdevice *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "legacy-pairing", value, NULL);
}

/**
 * bluez_device__get_rssi: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.RSSI">"RSSI"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint16 
bluez_device__get_rssi (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_rssi (object);
}

/**
 * bluez_device__set_rssi: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.RSSI">"RSSI"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_rssi (bluezdevice *object, gint16 value)
{
  g_object_set (G_OBJECT (object), "rssi", value, NULL);
}

/**
 * bluez_device__get_connected: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device__get_connected (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_connected (object);
}

/**
 * bluez_device__set_connected: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_connected (bluezdevice *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "connected", value, NULL);
}

/**
 * bluez_device__get_uuids: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_uuids() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
bluez_device__get_uuids (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_uuids (object);
}

/**
 * bluez_device__dup_uuids: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
bluez_device__dup_uuids (bluezdevice *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "uuids", &value, NULL);
  return value;
}

/**
 * bluez_device__set_uuids: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.UUIDs">"UUIDs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_uuids (bluezdevice *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "uuids", value, NULL);
}

/**
 * bluez_device__get_modalias: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_modalias() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device__get_modalias (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_modalias (object);
}

/**
 * bluez_device__dup_modalias: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device__dup_modalias (bluezdevice *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "modalias", &value, NULL);
  return value;
}

/**
 * bluez_device__set_modalias: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Modalias">"Modalias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_modalias (bluezdevice *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "modalias", value, NULL);
}

/**
 * bluez_device__get_adapter: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_adapter() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device__get_adapter (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_adapter (object);
}

/**
 * bluez_device__dup_adapter: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device__dup_adapter (bluezdevice *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "adapter", &value, NULL);
  return value;
}

/**
 * bluez_device__set_adapter: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_adapter (bluezdevice *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "adapter", value, NULL);
}

/**
 * bluez_device__get_manufacturer_data: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.ManufacturerData">"ManufacturerData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_manufacturer_data() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_device__get_manufacturer_data (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_manufacturer_data (object);
}

/**
 * bluez_device__dup_manufacturer_data: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.ManufacturerData">"ManufacturerData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_device__dup_manufacturer_data (bluezdevice *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "manufacturer-data", &value, NULL);
  return value;
}

/**
 * bluez_device__set_manufacturer_data: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.ManufacturerData">"ManufacturerData"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_manufacturer_data (bluezdevice *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "manufacturer-data", value, NULL);
}

/**
 * bluez_device__get_service_data: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_service_data() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_device__get_service_data (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_service_data (object);
}

/**
 * bluez_device__dup_service_data: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_device__dup_service_data (bluezdevice *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "service-data", &value, NULL);
  return value;
}

/**
 * bluez_device__set_service_data: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_service_data (bluezdevice *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "service-data", value, NULL);
}

/**
 * bluez_device__get_tx_power: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.TxPower">"TxPower"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint16 
bluez_device__get_tx_power (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_tx_power (object);
}

/**
 * bluez_device__set_tx_power: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.TxPower">"TxPower"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_tx_power (bluezdevice *object, gint16 value)
{
  g_object_set (G_OBJECT (object), "tx-power", value, NULL);
}

/**
 * bluez_device__get_gatt_services: (skip)
 * @object: A #bluezdevice.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device__dup_gatt_services() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
bluez_device__get_gatt_services (bluezdevice *object)
{
  return BLUEZ_DEVICE__GET_IFACE (object)->get_gatt_services (object);
}

/**
 * bluez_device__dup_gatt_services: (skip)
 * @object: A #bluezdevice.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
bluez_device__dup_gatt_services (bluezdevice *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "gatt-services", &value, NULL);
  return value;
}

/**
 * bluez_device__set_gatt_services: (skip)
 * @object: A #bluezdevice.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.GattServices">"GattServices"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device__set_gatt_services (bluezdevice *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "gatt-services", value, NULL);
}

/**
 * bluez_device__call_disconnect:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__call_disconnect_finish() to get the result of the operation.
 *
 * See bluez_device__call_disconnect_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device__call_disconnect (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device__call_disconnect_finish:
 * @proxy: A #bluezdeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__call_disconnect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device__call_disconnect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_disconnect_finish (
    bluezdevice *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_disconnect_sync:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device__call_disconnect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_disconnect_sync (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_connect:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__call_connect_finish() to get the result of the operation.
 *
 * See bluez_device__call_connect_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device__call_connect (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Connect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device__call_connect_finish:
 * @proxy: A #bluezdeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__call_connect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device__call_connect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_connect_finish (
    bluezdevice *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_connect_sync:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device__call_connect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_connect_sync (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Connect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_connect_profile:
 * @proxy: A #bluezdeviceProxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__call_connect_profile_finish() to get the result of the operation.
 *
 * See bluez_device__call_connect_profile_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device__call_connect_profile (
    bluezdevice *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ConnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device__call_connect_profile_finish:
 * @proxy: A #bluezdeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__call_connect_profile().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device__call_connect_profile().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_connect_profile_finish (
    bluezdevice *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_connect_profile_sync:
 * @proxy: A #bluezdeviceProxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device__call_connect_profile() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_connect_profile_sync (
    bluezdevice *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ConnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_disconnect_profile:
 * @proxy: A #bluezdeviceProxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__call_disconnect_profile_finish() to get the result of the operation.
 *
 * See bluez_device__call_disconnect_profile_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device__call_disconnect_profile (
    bluezdevice *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "DisconnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device__call_disconnect_profile_finish:
 * @proxy: A #bluezdeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__call_disconnect_profile().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device__call_disconnect_profile().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_disconnect_profile_finish (
    bluezdevice *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_disconnect_profile_sync:
 * @proxy: A #bluezdeviceProxy.
 * @arg_UUID: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device__call_disconnect_profile() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_disconnect_profile_sync (
    bluezdevice *proxy,
    const gchar *arg_UUID,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "DisconnectProfile",
    g_variant_new ("(s)",
                   arg_UUID),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_pair:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__call_pair_finish() to get the result of the operation.
 *
 * See bluez_device__call_pair_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device__call_pair (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Pair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device__call_pair_finish:
 * @proxy: A #bluezdeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__call_pair().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device__call_pair().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_pair_finish (
    bluezdevice *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_pair_sync:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device__call_pair() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_pair_sync (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Pair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_cancel_pairing:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__call_cancel_pairing_finish() to get the result of the operation.
 *
 * See bluez_device__call_cancel_pairing_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device__call_cancel_pairing (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "CancelPairing",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device__call_cancel_pairing_finish:
 * @proxy: A #bluezdeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__call_cancel_pairing().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device__call_cancel_pairing().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_cancel_pairing_finish (
    bluezdevice *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__call_cancel_pairing_sync:
 * @proxy: A #bluezdeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device__call_cancel_pairing() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device__call_cancel_pairing_sync (
    bluezdevice *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "CancelPairing",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device__complete_disconnect:
 * @object: A #bluezdevice.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device__complete_disconnect (
    bluezdevice *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device__complete_connect:
 * @object: A #bluezdevice.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device__complete_connect (
    bluezdevice *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device__complete_connect_profile:
 * @object: A #bluezdevice.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.ConnectProfile">ConnectProfile()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device__complete_connect_profile (
    bluezdevice *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device__complete_disconnect_profile:
 * @object: A #bluezdevice.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.DisconnectProfile">DisconnectProfile()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device__complete_disconnect_profile (
    bluezdevice *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device__complete_pair:
 * @object: A #bluezdevice.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Pair">Pair()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device__complete_pair (
    bluezdevice *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device__complete_cancel_pairing:
 * @object: A #bluezdevice.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.CancelPairing">CancelPairing()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device__complete_cancel_pairing (
    bluezdevice *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceProxy:
 *
 * The #bluezdeviceProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceProxy.
 */

struct _bluezdeviceProxyPrivate
{
  GData *qdata;
};

static void bluez_device__proxy_iface_init (bluezdeviceIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceProxy, bluez_device__proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (bluezdeviceProxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_, bluez_device__proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceProxy, bluez_device__proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_, bluez_device__proxy_iface_init));

#endif
static void
bluez_device__proxy_finalize (GObject *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_device__proxy_parent_class)->finalize (object);
}

static void
bluez_device__proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 19);
  info = _bluez_device__property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_device__proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.Device1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_device__proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 19);
  info = _bluez_device__property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.Device1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_device__proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_device__proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_device__interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_DEVICE_TYPE_);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_device__proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device__interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device__interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_device__proxy_get_address (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Address");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device__proxy_get_name (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Name");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device__proxy_get_alias (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Alias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
bluez_device__proxy_get_class (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Class");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint16 
bluez_device__proxy_get_appearance (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  guint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Appearance");
  if (variant != NULL)
    {
      value = g_variant_get_uint16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device__proxy_get_icon (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Icon");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_device__proxy_get_paired (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Paired");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_device__proxy_get_trusted (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Trusted");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_device__proxy_get_blocked (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Blocked");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_device__proxy_get_legacy_pairing (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LegacyPairing");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint16 
bluez_device__proxy_get_rssi (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "RSSI");
  if (variant != NULL)
    {
      value = g_variant_get_int16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_device__proxy_get_connected (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Connected");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
bluez_device__proxy_get_uuids (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "UUIDs");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UUIDs");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "UUIDs", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device__proxy_get_modalias (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Modalias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device__proxy_get_adapter (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Adapter");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
bluez_device__proxy_get_manufacturer_data (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ManufacturerData");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static GVariant *
bluez_device__proxy_get_service_data (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ServiceData");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static gint16 
bluez_device__proxy_get_tx_power (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  gint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "TxPower");
  if (variant != NULL)
    {
      value = g_variant_get_int16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
bluez_device__proxy_get_gatt_services (bluezdevice *object)
{
  bluezdeviceProxy *proxy = BLUEZ_DEVICE__PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "GattServices");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_device__proxy_init (bluezdeviceProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_device__proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_DEVICE_TYPE__PROXY, bluezdeviceProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_device__interface_info ());
}

static void
bluez_device__proxy_class_init (bluezdeviceProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_device__proxy_finalize;
  gobject_class->get_property = bluez_device__proxy_get_property;
  gobject_class->set_property = bluez_device__proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_device__proxy_g_signal;
  proxy_class->g_properties_changed = bluez_device__proxy_g_properties_changed;

  bluez_device__override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceProxyPrivate));
#endif
}

static void
bluez_device__proxy_iface_init (bluezdeviceIface *iface)
{
  iface->get_address = bluez_device__proxy_get_address;
  iface->get_name = bluez_device__proxy_get_name;
  iface->get_alias = bluez_device__proxy_get_alias;
  iface->get_class = bluez_device__proxy_get_class;
  iface->get_appearance = bluez_device__proxy_get_appearance;
  iface->get_icon = bluez_device__proxy_get_icon;
  iface->get_paired = bluez_device__proxy_get_paired;
  iface->get_trusted = bluez_device__proxy_get_trusted;
  iface->get_blocked = bluez_device__proxy_get_blocked;
  iface->get_legacy_pairing = bluez_device__proxy_get_legacy_pairing;
  iface->get_rssi = bluez_device__proxy_get_rssi;
  iface->get_connected = bluez_device__proxy_get_connected;
  iface->get_uuids = bluez_device__proxy_get_uuids;
  iface->get_modalias = bluez_device__proxy_get_modalias;
  iface->get_adapter = bluez_device__proxy_get_adapter;
  iface->get_manufacturer_data = bluez_device__proxy_get_manufacturer_data;
  iface->get_service_data = bluez_device__proxy_get_service_data;
  iface->get_tx_power = bluez_device__proxy_get_tx_power;
  iface->get_gatt_services = bluez_device__proxy_get_gatt_services;
}

/**
 * bluez_device__proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__proxy_new_finish() to get the result of the operation.
 *
 * See bluez_device__proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device__proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE__PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
}

/**
 * bluez_device__proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device__proxy_new().
 *
 * Returns: (transfer full) (type bluezdeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdevice *
bluez_device__proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ (ret);
  else
    return NULL;
}

/**
 * bluez_device__proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device__proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdevice *
bluez_device__proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE__PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ (ret);
  else
    return NULL;
}


/**
 * bluez_device__proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_device__proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device__proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_device__proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device__proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE__PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
}

/**
 * bluez_device__proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device__proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device__proxy_new_for_bus().
 *
 * Returns: (transfer full) (type bluezdeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdevice *
bluez_device__proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ (ret);
  else
    return NULL;
}

/**
 * bluez_device__proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_device__proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device__proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdevice *
bluez_device__proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE__PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceSkeleton:
 *
 * The #bluezdeviceSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceSkeleton.
 */

struct _bluezdeviceSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_device__skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_DEVICE_TYPE_);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_device__skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device__interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_device__skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device__interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_device__skeleton_vtable =
{
  _bluez_device__skeleton_handle_method_call,
  _bluez_device__skeleton_handle_get_property,
  _bluez_device__skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_device__skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_device__interface_info ();
}

static GDBusInterfaceVTable *
bluez_device__skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_device__skeleton_vtable;
}

static GVariant *
bluez_device__skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_device__interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_device__interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_device__interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_device__skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.Device1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_device__emit_changed (gpointer user_data);

static void
bluez_device__skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_device__emit_changed (skeleton);
}

static void bluez_device__skeleton_iface_init (bluezdeviceIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceSkeleton, bluez_device__skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (bluezdeviceSkeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_, bluez_device__skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceSkeleton, bluez_device__skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_, bluez_device__skeleton_iface_init));

#endif
static void
bluez_device__skeleton_finalize (GObject *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  guint n;
  for (n = 0; n < 19; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_device__skeleton_parent_class)->finalize (object);
}

static void
bluez_device__skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 19);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_device__emit_changed (gpointer user_data)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.Device1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_device__schedule_emit_changed (bluezdeviceSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_device__skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_device__emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_device__skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 19);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _bluez_device__schedule_emit_changed (skeleton, _bluez_device__property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_device__skeleton_init (bluezdeviceSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_device__skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_DEVICE_TYPE__SKELETON, bluezdeviceSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 19);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[15], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[16], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[17], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[18], G_TYPE_STRV);
}

static const gchar *
bluez_device__skeleton_get_address (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device__skeleton_get_name (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device__skeleton_get_alias (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
bluez_device__skeleton_get_class (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint16 
bluez_device__skeleton_get_appearance (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device__skeleton_get_icon (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device__skeleton_get_paired (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device__skeleton_get_trusted (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device__skeleton_get_blocked (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device__skeleton_get_legacy_pairing (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint16 
bluez_device__skeleton_get_rssi (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device__skeleton_get_connected (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
bluez_device__skeleton_get_uuids (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device__skeleton_get_modalias (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device__skeleton_get_adapter (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_device__skeleton_get_manufacturer_data (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[15]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_device__skeleton_get_service_data (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[16]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint16 
bluez_device__skeleton_get_tx_power (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[17]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
bluez_device__skeleton_get_gatt_services (bluezdevice *object)
{
  bluezdeviceSkeleton *skeleton = BLUEZ_DEVICE__SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[18]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_device__skeleton_class_init (bluezdeviceSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_device__skeleton_finalize;
  gobject_class->get_property = bluez_device__skeleton_get_property;
  gobject_class->set_property = bluez_device__skeleton_set_property;
  gobject_class->notify       = bluez_device__skeleton_notify;


  bluez_device__override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_device__skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_device__skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_device__skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_device__skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceSkeletonPrivate));
#endif
}

static void
bluez_device__skeleton_iface_init (bluezdeviceIface *iface)
{
  iface->get_address = bluez_device__skeleton_get_address;
  iface->get_name = bluez_device__skeleton_get_name;
  iface->get_alias = bluez_device__skeleton_get_alias;
  iface->get_class = bluez_device__skeleton_get_class;
  iface->get_appearance = bluez_device__skeleton_get_appearance;
  iface->get_icon = bluez_device__skeleton_get_icon;
  iface->get_paired = bluez_device__skeleton_get_paired;
  iface->get_trusted = bluez_device__skeleton_get_trusted;
  iface->get_blocked = bluez_device__skeleton_get_blocked;
  iface->get_legacy_pairing = bluez_device__skeleton_get_legacy_pairing;
  iface->get_rssi = bluez_device__skeleton_get_rssi;
  iface->get_connected = bluez_device__skeleton_get_connected;
  iface->get_uuids = bluez_device__skeleton_get_uuids;
  iface->get_modalias = bluez_device__skeleton_get_modalias;
  iface->get_adapter = bluez_device__skeleton_get_adapter;
  iface->get_manufacturer_data = bluez_device__skeleton_get_manufacturer_data;
  iface->get_service_data = bluez_device__skeleton_get_service_data;
  iface->get_tx_power = bluez_device__skeleton_get_tx_power;
  iface->get_gatt_services = bluez_device__skeleton_get_gatt_services;
}

/**
 * bluez_device__skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 *
 * Returns: (transfer full) (type bluezdeviceSkeleton): The skeleton object.
 */
bluezdevice *
bluez_device__skeleton_new (void)
{
  return BLUEZ_DEVICE_ (g_object_new (BLUEZ_DEVICE_TYPE__SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Properties
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:bluezdeviceOrgFreedesktopDBusProperties
 * @title: bluezdeviceOrgFreedesktopDBusProperties
 * @short_description: Generated C code for the org.freedesktop.DBus.Properties D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Properties ---- */

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get_IN_ARG_interface =
{
  {
    -1,
    (gchar *) "interface",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_properties_method_info_get_IN_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get_IN_ARG_interface,
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_value,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get =
{
  {
    -1,
    (gchar *) "Get",
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_properties_method_info_get_IN_ARG_pointers,
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_pointers,
    NULL
  },
  "handle-get",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_interface =
{
  {
    -1,
    (gchar *) "interface",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_interface,
  &_bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_name,
  &_bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_value,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_device_org_freedesktop_dbus_properties_method_info_set =
{
  {
    -1,
    (gchar *) "Set",
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_properties_method_info_set_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_interface =
{
  {
    -1,
    (gchar *) "interface",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_interface,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_properties =
{
  {
    -1,
    (gchar *) "properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_properties,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_device_org_freedesktop_dbus_properties_method_info_get_all =
{
  {
    -1,
    (gchar *) "GetAll",
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_pointers,
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_pointers,
    NULL
  },
  "handle-get-all",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _bluez_device_org_freedesktop_dbus_properties_method_info_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get,
  &_bluez_device_org_freedesktop_dbus_properties_method_info_set,
  &_bluez_device_org_freedesktop_dbus_properties_method_info_get_all,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_interface =
{
  {
    -1,
    (gchar *) "interface",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_changed_properties =
{
  {
    -1,
    (gchar *) "changed_properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_invalidated_properties =
{
  {
    -1,
    (gchar *) "invalidated_properties",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_interface,
  &_bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_changed_properties,
  &_bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_invalidated_properties,
  NULL
};

static const _ExtendedGDBusSignalInfo _bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed =
{
  {
    -1,
    (gchar *) "PropertiesChanged",
    (GDBusArgInfo **) &_bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_pointers,
    NULL
  },
  "properties-changed"
};

static const _ExtendedGDBusSignalInfo * const _bluez_device_org_freedesktop_dbus_properties_signal_info_pointers[] =
{
  &_bluez_device_org_freedesktop_dbus_properties_signal_info_properties_changed,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_device_org_freedesktop_dbus_properties_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.DBus.Properties",
    (GDBusMethodInfo **) &_bluez_device_org_freedesktop_dbus_properties_method_info_pointers,
    (GDBusSignalInfo **) &_bluez_device_org_freedesktop_dbus_properties_signal_info_pointers,
    NULL,
    NULL
  },
  "org-freedesktop-dbus-properties",
};


/**
 * bluez_device_org_freedesktop_dbus_properties_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_device_org_freedesktop_dbus_properties_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #bluezdeviceOrgFreedesktopDBusProperties interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_device_org_freedesktop_dbus_properties_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * bluezdeviceOrgFreedesktopDBusProperties:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 */

/**
 * bluezdeviceOrgFreedesktopDBusPropertiesIface:
 * @parent_iface: The parent interface.
 * @handle_get: Handler for the #bluezdeviceOrgFreedesktopDBusProperties::handle-get signal.
 * @handle_get_all: Handler for the #bluezdeviceOrgFreedesktopDBusProperties::handle-get-all signal.
 * @handle_set: Handler for the #bluezdeviceOrgFreedesktopDBusProperties::handle-set signal.
 * @properties_changed: Handler for the #bluezdeviceOrgFreedesktopDBusProperties::properties-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 */

typedef bluezdeviceOrgFreedesktopDBusPropertiesIface bluezdeviceOrgFreedesktopDBusPropertiesInterface;
G_DEFINE_INTERFACE (bluezdeviceOrgFreedesktopDBusProperties, bluez_device_org_freedesktop_dbus_properties, G_TYPE_OBJECT);

static void
bluez_device_org_freedesktop_dbus_properties_default_init (bluezdeviceOrgFreedesktopDBusPropertiesIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * bluezdeviceOrgFreedesktopDBusProperties::handle-get:
   * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interface: Argument passed by remote caller.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device_org_freedesktop_dbus_properties_complete_get() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceOrgFreedesktopDBusPropertiesIface, handle_get),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * bluezdeviceOrgFreedesktopDBusProperties::handle-set:
   * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interface: Argument passed by remote caller.
   * @arg_name: Argument passed by remote caller.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device_org_freedesktop_dbus_properties_complete_set() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceOrgFreedesktopDBusPropertiesIface, handle_set),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * bluezdeviceOrgFreedesktopDBusProperties::handle-get-all:
   * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interface: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device_org_freedesktop_dbus_properties_complete_get_all() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-all",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceOrgFreedesktopDBusPropertiesIface, handle_get_all),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject signals for received D-Bus signals: */
  /**
   * bluezdeviceOrgFreedesktopDBusProperties::properties-changed:
   * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
   * @arg_interface: Argument.
   * @arg_changed_properties: Argument.
   * @arg_invalidated_properties: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-DBus-Properties.PropertiesChanged">"PropertiesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("properties-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (bluezdeviceOrgFreedesktopDBusPropertiesIface, properties_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_STRING, G_TYPE_VARIANT, G_TYPE_STRV);

}

/**
 * bluez_device_org_freedesktop_dbus_properties_emit_properties_changed:
 * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
 * @arg_interface: Argument to pass with the signal.
 * @arg_changed_properties: Argument to pass with the signal.
 * @arg_invalidated_properties: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-DBus-Properties.PropertiesChanged">"PropertiesChanged"</link> D-Bus signal.
 */
void
bluez_device_org_freedesktop_dbus_properties_emit_properties_changed (
    bluezdeviceOrgFreedesktopDBusProperties *object,
    const gchar *arg_interface,
    GVariant *arg_changed_properties,
    const gchar *const *arg_invalidated_properties)
{
  g_signal_emit_by_name (object, "properties-changed", arg_interface, arg_changed_properties, arg_invalidated_properties);
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_get:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_properties_call_get_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_properties_call_get_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device_org_freedesktop_dbus_properties_call_get (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Get",
    g_variant_new ("(ss)",
                   arg_interface,
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_get_finish:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @out_value: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_properties_call_get().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_properties_call_get().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_properties_call_get_finish (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    GVariant **out_value,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@v)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_get_sync:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @out_value: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_properties_call_get() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_properties_call_get_sync (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface,
    const gchar *arg_name,
    GVariant **out_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Get",
    g_variant_new ("(ss)",
                   arg_interface,
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@v)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_set:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_properties_call_set_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_properties_call_set_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device_org_freedesktop_dbus_properties_call_set (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface,
    const gchar *arg_name,
    GVariant *arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Set",
    g_variant_new ("(ss@v)",
                   arg_interface,
                   arg_name,
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_set_finish:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_properties_call_set().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_properties_call_set().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_properties_call_set_finish (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_set_sync:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface: Argument to pass with the method invocation.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_properties_call_set() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_properties_call_set_sync (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface,
    const gchar *arg_name,
    GVariant *arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Set",
    g_variant_new ("(ss@v)",
                   arg_interface,
                   arg_name,
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_get_all:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_properties_call_get_all_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_properties_call_get_all_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device_org_freedesktop_dbus_properties_call_get_all (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetAll",
    g_variant_new ("(s)",
                   arg_interface),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_get_all_finish:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @out_properties: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_properties_call_get_all().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_properties_call_get_all().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_properties_call_get_all_finish (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    GVariant **out_properties,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a{sv})",
                 out_properties);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_call_get_all_sync:
 * @proxy: A #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface: Argument to pass with the method invocation.
 * @out_properties: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_properties_call_get_all() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
bluez_device_org_freedesktop_dbus_properties_call_get_all_sync (
    bluezdeviceOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface,
    GVariant **out_properties,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetAll",
    g_variant_new ("(s)",
                   arg_interface),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a{sv})",
                 out_properties);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_complete_get:
 * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @value: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device_org_freedesktop_dbus_properties_complete_get (
    bluezdeviceOrgFreedesktopDBusProperties *object,
    GDBusMethodInvocation *invocation,
    GVariant *value)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@v)",
                   value));
}

/**
 * bluez_device_org_freedesktop_dbus_properties_complete_set:
 * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device_org_freedesktop_dbus_properties_complete_set (
    bluezdeviceOrgFreedesktopDBusProperties *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device_org_freedesktop_dbus_properties_complete_get_all:
 * @object: A #bluezdeviceOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @properties: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device_org_freedesktop_dbus_properties_complete_get_all (
    bluezdeviceOrgFreedesktopDBusProperties *object,
    GDBusMethodInvocation *invocation,
    GVariant *properties)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a{sv})",
                   properties));
}

/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceOrgFreedesktopDBusPropertiesProxy:
 *
 * The #bluezdeviceOrgFreedesktopDBusPropertiesProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceOrgFreedesktopDBusPropertiesProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceOrgFreedesktopDBusPropertiesProxy.
 */

struct _bluezdeviceOrgFreedesktopDBusPropertiesProxyPrivate
{
  GData *qdata;
};

static void bluez_device_org_freedesktop_dbus_properties_proxy_iface_init (bluezdeviceOrgFreedesktopDBusPropertiesIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusPropertiesProxy, bluez_device_org_freedesktop_dbus_properties_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (bluezdeviceOrgFreedesktopDBusPropertiesProxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, bluez_device_org_freedesktop_dbus_properties_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusPropertiesProxy, bluez_device_org_freedesktop_dbus_properties_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, bluez_device_org_freedesktop_dbus_properties_proxy_iface_init));

#endif
static void
bluez_device_org_freedesktop_dbus_properties_proxy_finalize (GObject *object)
{
  bluezdeviceOrgFreedesktopDBusPropertiesProxy *proxy = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_device_org_freedesktop_dbus_properties_proxy_parent_class)->finalize (object);
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  bluezdeviceOrgFreedesktopDBusPropertiesProxy *proxy = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_init (bluezdeviceOrgFreedesktopDBusPropertiesProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_device_org_freedesktop_dbus_properties_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, bluezdeviceOrgFreedesktopDBusPropertiesProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_device_org_freedesktop_dbus_properties_interface_info ());
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_class_init (bluezdeviceOrgFreedesktopDBusPropertiesProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_device_org_freedesktop_dbus_properties_proxy_finalize;
  gobject_class->get_property = bluez_device_org_freedesktop_dbus_properties_proxy_get_property;
  gobject_class->set_property = bluez_device_org_freedesktop_dbus_properties_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_device_org_freedesktop_dbus_properties_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_device_org_freedesktop_dbus_properties_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceOrgFreedesktopDBusPropertiesProxyPrivate));
#endif
}

static void
bluez_device_org_freedesktop_dbus_properties_proxy_iface_init (bluezdeviceOrgFreedesktopDBusPropertiesIface *iface)
{
}

/**
 * bluez_device_org_freedesktop_dbus_properties_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_properties_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_properties_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device_org_freedesktop_dbus_properties_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
}

/**
 * bluez_device_org_freedesktop_dbus_properties_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_properties_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_properties_proxy_new().
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusProperties *
bluez_device_org_freedesktop_dbus_properties_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_properties_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusProperties *
bluez_device_org_freedesktop_dbus_properties_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}


/**
 * bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_device_org_freedesktop_dbus_properties_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
}

/**
 * bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusProperties *
bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_device_org_freedesktop_dbus_properties_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgFreedesktopDBusProperties *
bluez_device_org_freedesktop_dbus_properties_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceOrgFreedesktopDBusPropertiesSkeleton:
 *
 * The #bluezdeviceOrgFreedesktopDBusPropertiesSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceOrgFreedesktopDBusPropertiesSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceOrgFreedesktopDBusPropertiesSkeleton.
 */

struct _bluezdeviceOrgFreedesktopDBusPropertiesSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_device_org_freedesktop_dbus_properties_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_device_org_freedesktop_dbus_properties_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_device_org_freedesktop_dbus_properties_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_device_org_freedesktop_dbus_properties_skeleton_vtable =
{
  _bluez_device_org_freedesktop_dbus_properties_skeleton_handle_method_call,
  _bluez_device_org_freedesktop_dbus_properties_skeleton_handle_get_property,
  _bluez_device_org_freedesktop_dbus_properties_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_device_org_freedesktop_dbus_properties_interface_info ();
}

static GDBusInterfaceVTable *
bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_device_org_freedesktop_dbus_properties_skeleton_vtable;
}

static GVariant *
bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_device_org_freedesktop_dbus_properties_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_device_org_freedesktop_dbus_properties_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Properties", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void
_bluez_device_org_freedesktop_dbus_properties_on_signal_properties_changed (
    bluezdeviceOrgFreedesktopDBusProperties *object,
    const gchar *arg_interface,
    GVariant *arg_changed_properties,
    const gchar *const *arg_invalidated_properties)
{
  bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s@a{sv}^as)",
                   arg_interface,
                   arg_changed_properties,
                   arg_invalidated_properties));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Properties", "PropertiesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void bluez_device_org_freedesktop_dbus_properties_skeleton_iface_init (bluezdeviceOrgFreedesktopDBusPropertiesIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusPropertiesSkeleton, bluez_device_org_freedesktop_dbus_properties_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (bluezdeviceOrgFreedesktopDBusPropertiesSkeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, bluez_device_org_freedesktop_dbus_properties_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgFreedesktopDBusPropertiesSkeleton, bluez_device_org_freedesktop_dbus_properties_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, bluez_device_org_freedesktop_dbus_properties_skeleton_iface_init));

#endif
static void
bluez_device_org_freedesktop_dbus_properties_skeleton_finalize (GObject *object)
{
  bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton = BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_device_org_freedesktop_dbus_properties_skeleton_parent_class)->finalize (object);
}

static void
bluez_device_org_freedesktop_dbus_properties_skeleton_init (bluezdeviceOrgFreedesktopDBusPropertiesSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_device_org_freedesktop_dbus_properties_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON, bluezdeviceOrgFreedesktopDBusPropertiesSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
bluez_device_org_freedesktop_dbus_properties_skeleton_class_init (bluezdeviceOrgFreedesktopDBusPropertiesSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_device_org_freedesktop_dbus_properties_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_device_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceOrgFreedesktopDBusPropertiesSkeletonPrivate));
#endif
}

static void
bluez_device_org_freedesktop_dbus_properties_skeleton_iface_init (bluezdeviceOrgFreedesktopDBusPropertiesIface *iface)
{
  iface->properties_changed = _bluez_device_org_freedesktop_dbus_properties_on_signal_properties_changed;
}

/**
 * bluez_device_org_freedesktop_dbus_properties_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 *
 * Returns: (transfer full) (type bluezdeviceOrgFreedesktopDBusPropertiesSkeleton): The skeleton object.
 */
bluezdeviceOrgFreedesktopDBusProperties *
bluez_device_org_freedesktop_dbus_properties_skeleton_new (void)
{
  return BLUEZ_DEVICE_ORG_FREEDESKTOP_DBUS_PROPERTIES (g_object_new (BLUEZ_DEVICE_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.ProximityReporter1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:bluezdeviceOrgBluezProximityReporter1
 * @title: bluezdeviceOrgBluezProximityReporter1
 * @short_description: Generated C code for the org.bluez.ProximityReporter1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.ProximityReporter1 ---- */

static const _ExtendedGDBusPropertyInfo _bluez_device_org_bluez_proximity_reporter1_property_info_link_loss_alert_level =
{
  {
    -1,
    (gchar *) "LinkLossAlertLevel",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "link-loss-alert-level",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _bluez_device_org_bluez_proximity_reporter1_property_info_immediate_alert_level =
{
  {
    -1,
    (gchar *) "ImmediateAlertLevel",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "immediate-alert-level",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _bluez_device_org_bluez_proximity_reporter1_property_info_pointers[] =
{
  &_bluez_device_org_bluez_proximity_reporter1_property_info_link_loss_alert_level,
  &_bluez_device_org_bluez_proximity_reporter1_property_info_immediate_alert_level,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_device_org_bluez_proximity_reporter1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.ProximityReporter1",
    NULL,
    NULL,
    (GDBusPropertyInfo **) &_bluez_device_org_bluez_proximity_reporter1_property_info_pointers,
    NULL
  },
  "org-bluez-proximity-reporter1",
};


/**
 * bluez_device_org_bluez_proximity_reporter1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_device_org_bluez_proximity_reporter1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #bluezdeviceOrgBluezProximityReporter1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_device_org_bluez_proximity_reporter1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "link-loss-alert-level");
  g_object_class_override_property (klass, property_id_begin++, "immediate-alert-level");
  return property_id_begin - 1;
}



/**
 * bluezdeviceOrgBluezProximityReporter1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link>.
 */

/**
 * bluezdeviceOrgBluezProximityReporter1Iface:
 * @parent_iface: The parent interface.
 * @get_immediate_alert_level: Getter for the #bluezdeviceOrgBluezProximityReporter1:immediate-alert-level property.
 * @get_link_loss_alert_level: Getter for the #bluezdeviceOrgBluezProximityReporter1:link-loss-alert-level property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link>.
 */

typedef bluezdeviceOrgBluezProximityReporter1Iface bluezdeviceOrgBluezProximityReporter1Interface;
G_DEFINE_INTERFACE (bluezdeviceOrgBluezProximityReporter1, bluez_device_org_bluez_proximity_reporter1, G_TYPE_OBJECT);

static void
bluez_device_org_bluez_proximity_reporter1_default_init (bluezdeviceOrgBluezProximityReporter1Iface *iface)
{
  /* GObject properties for D-Bus properties: */
  /**
   * bluezdeviceOrgBluezProximityReporter1:link-loss-alert-level:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-ProximityReporter1.LinkLossAlertLevel">"LinkLossAlertLevel"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("link-loss-alert-level", "LinkLossAlertLevel", "LinkLossAlertLevel", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * bluezdeviceOrgBluezProximityReporter1:immediate-alert-level:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-ProximityReporter1.ImmediateAlertLevel">"ImmediateAlertLevel"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("immediate-alert-level", "ImmediateAlertLevel", "ImmediateAlertLevel", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_device_org_bluez_proximity_reporter1_get_link_loss_alert_level: (skip)
 * @object: A #bluezdeviceOrgBluezProximityReporter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-ProximityReporter1.LinkLossAlertLevel">"LinkLossAlertLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device_org_bluez_proximity_reporter1_dup_link_loss_alert_level() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device_org_bluez_proximity_reporter1_get_link_loss_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_GET_IFACE (object)->get_link_loss_alert_level (object);
}

/**
 * bluez_device_org_bluez_proximity_reporter1_dup_link_loss_alert_level: (skip)
 * @object: A #bluezdeviceOrgBluezProximityReporter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-ProximityReporter1.LinkLossAlertLevel">"LinkLossAlertLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device_org_bluez_proximity_reporter1_dup_link_loss_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "link-loss-alert-level", &value, NULL);
  return value;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_set_link_loss_alert_level: (skip)
 * @object: A #bluezdeviceOrgBluezProximityReporter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-ProximityReporter1.LinkLossAlertLevel">"LinkLossAlertLevel"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device_org_bluez_proximity_reporter1_set_link_loss_alert_level (bluezdeviceOrgBluezProximityReporter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "link-loss-alert-level", value, NULL);
}

/**
 * bluez_device_org_bluez_proximity_reporter1_get_immediate_alert_level: (skip)
 * @object: A #bluezdeviceOrgBluezProximityReporter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-ProximityReporter1.ImmediateAlertLevel">"ImmediateAlertLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device_org_bluez_proximity_reporter1_dup_immediate_alert_level() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device_org_bluez_proximity_reporter1_get_immediate_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_GET_IFACE (object)->get_immediate_alert_level (object);
}

/**
 * bluez_device_org_bluez_proximity_reporter1_dup_immediate_alert_level: (skip)
 * @object: A #bluezdeviceOrgBluezProximityReporter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-ProximityReporter1.ImmediateAlertLevel">"ImmediateAlertLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device_org_bluez_proximity_reporter1_dup_immediate_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "immediate-alert-level", &value, NULL);
  return value;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_set_immediate_alert_level: (skip)
 * @object: A #bluezdeviceOrgBluezProximityReporter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-ProximityReporter1.ImmediateAlertLevel">"ImmediateAlertLevel"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device_org_bluez_proximity_reporter1_set_immediate_alert_level (bluezdeviceOrgBluezProximityReporter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "immediate-alert-level", value, NULL);
}

/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceOrgBluezProximityReporter1Proxy:
 *
 * The #bluezdeviceOrgBluezProximityReporter1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceOrgBluezProximityReporter1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceOrgBluezProximityReporter1Proxy.
 */

struct _bluezdeviceOrgBluezProximityReporter1ProxyPrivate
{
  GData *qdata;
};

static void bluez_device_org_bluez_proximity_reporter1_proxy_iface_init (bluezdeviceOrgBluezProximityReporter1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgBluezProximityReporter1Proxy, bluez_device_org_bluez_proximity_reporter1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (bluezdeviceOrgBluezProximityReporter1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1, bluez_device_org_bluez_proximity_reporter1_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgBluezProximityReporter1Proxy, bluez_device_org_bluez_proximity_reporter1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1, bluez_device_org_bluez_proximity_reporter1_proxy_iface_init));

#endif
static void
bluez_device_org_bluez_proximity_reporter1_proxy_finalize (GObject *object)
{
  bluezdeviceOrgBluezProximityReporter1Proxy *proxy = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_device_org_bluez_proximity_reporter1_proxy_parent_class)->finalize (object);
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _bluez_device_org_bluez_proximity_reporter1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.ProximityReporter1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _bluez_device_org_bluez_proximity_reporter1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.ProximityReporter1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_device_org_bluez_proximity_reporter1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  bluezdeviceOrgBluezProximityReporter1Proxy *proxy = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_device_org_bluez_proximity_reporter1_proxy_get_link_loss_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  bluezdeviceOrgBluezProximityReporter1Proxy *proxy = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LinkLossAlertLevel");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device_org_bluez_proximity_reporter1_proxy_get_immediate_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  bluezdeviceOrgBluezProximityReporter1Proxy *proxy = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ImmediateAlertLevel");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_init (bluezdeviceOrgBluezProximityReporter1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_device_org_bluez_proximity_reporter1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY, bluezdeviceOrgBluezProximityReporter1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_device_org_bluez_proximity_reporter1_interface_info ());
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_class_init (bluezdeviceOrgBluezProximityReporter1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_device_org_bluez_proximity_reporter1_proxy_finalize;
  gobject_class->get_property = bluez_device_org_bluez_proximity_reporter1_proxy_get_property;
  gobject_class->set_property = bluez_device_org_bluez_proximity_reporter1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_device_org_bluez_proximity_reporter1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_device_org_bluez_proximity_reporter1_proxy_g_properties_changed;

  bluez_device_org_bluez_proximity_reporter1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceOrgBluezProximityReporter1ProxyPrivate));
#endif
}

static void
bluez_device_org_bluez_proximity_reporter1_proxy_iface_init (bluezdeviceOrgBluezProximityReporter1Iface *iface)
{
  iface->get_link_loss_alert_level = bluez_device_org_bluez_proximity_reporter1_proxy_get_link_loss_alert_level;
  iface->get_immediate_alert_level = bluez_device_org_bluez_proximity_reporter1_proxy_get_immediate_alert_level;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_bluez_proximity_reporter1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_device_org_bluez_proximity_reporter1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device_org_bluez_proximity_reporter1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.ProximityReporter1", NULL);
}

/**
 * bluez_device_org_bluez_proximity_reporter1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_bluez_proximity_reporter1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device_org_bluez_proximity_reporter1_proxy_new().
 *
 * Returns: (transfer full) (type bluezdeviceOrgBluezProximityReporter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgBluezProximityReporter1 *
bluez_device_org_bluez_proximity_reporter1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1 (ret);
  else
    return NULL;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_bluez_proximity_reporter1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceOrgBluezProximityReporter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgBluezProximityReporter1 *
bluez_device_org_bluez_proximity_reporter1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.ProximityReporter1", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1 (ret);
  else
    return NULL;
}


/**
 * bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_device_org_bluez_proximity_reporter1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.ProximityReporter1", NULL);
}

/**
 * bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type bluezdeviceOrgBluezProximityReporter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgBluezProximityReporter1 *
bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1 (ret);
  else
    return NULL;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_device_org_bluez_proximity_reporter1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type bluezdeviceOrgBluezProximityReporter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
bluezdeviceOrgBluezProximityReporter1 *
bluez_device_org_bluez_proximity_reporter1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.ProximityReporter1", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * bluezdeviceOrgBluezProximityReporter1Skeleton:
 *
 * The #bluezdeviceOrgBluezProximityReporter1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * bluezdeviceOrgBluezProximityReporter1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #bluezdeviceOrgBluezProximityReporter1Skeleton.
 */

struct _bluezdeviceOrgBluezProximityReporter1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_device_org_bluez_proximity_reporter1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_device_org_bluez_proximity_reporter1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_device_org_bluez_proximity_reporter1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_device_org_bluez_proximity_reporter1_skeleton_vtable =
{
  _bluez_device_org_bluez_proximity_reporter1_skeleton_handle_method_call,
  _bluez_device_org_bluez_proximity_reporter1_skeleton_handle_get_property,
  _bluez_device_org_bluez_proximity_reporter1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_device_org_bluez_proximity_reporter1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_device_org_bluez_proximity_reporter1_skeleton_vtable;
}

static GVariant *
bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_device_org_bluez_proximity_reporter1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_device_org_bluez_proximity_reporter1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.ProximityReporter1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_device_org_bluez_proximity_reporter1_emit_changed (gpointer user_data);

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_device_org_bluez_proximity_reporter1_emit_changed (skeleton);
}

static void bluez_device_org_bluez_proximity_reporter1_skeleton_iface_init (bluezdeviceOrgBluezProximityReporter1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgBluezProximityReporter1Skeleton, bluez_device_org_bluez_proximity_reporter1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (bluezdeviceOrgBluezProximityReporter1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1, bluez_device_org_bluez_proximity_reporter1_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (bluezdeviceOrgBluezProximityReporter1Skeleton, bluez_device_org_bluez_proximity_reporter1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1, bluez_device_org_bluez_proximity_reporter1_skeleton_iface_init));

#endif
static void
bluez_device_org_bluez_proximity_reporter1_skeleton_finalize (GObject *object)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_device_org_bluez_proximity_reporter1_skeleton_parent_class)->finalize (object);
}

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_device_org_bluez_proximity_reporter1_emit_changed (gpointer user_data)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.ProximityReporter1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_device_org_bluez_proximity_reporter1_schedule_emit_changed (bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_device_org_bluez_proximity_reporter1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _bluez_device_org_bluez_proximity_reporter1_schedule_emit_changed (skeleton, _bluez_device_org_bluez_proximity_reporter1_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_init (bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_device_org_bluez_proximity_reporter1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON, bluezdeviceOrgBluezProximityReporter1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
}

static const gchar *
bluez_device_org_bluez_proximity_reporter1_skeleton_get_link_loss_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device_org_bluez_proximity_reporter1_skeleton_get_immediate_alert_level (bluezdeviceOrgBluezProximityReporter1 *object)
{
  bluezdeviceOrgBluezProximityReporter1Skeleton *skeleton = BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_class_init (bluezdeviceOrgBluezProximityReporter1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_device_org_bluez_proximity_reporter1_skeleton_finalize;
  gobject_class->get_property = bluez_device_org_bluez_proximity_reporter1_skeleton_get_property;
  gobject_class->set_property = bluez_device_org_bluez_proximity_reporter1_skeleton_set_property;
  gobject_class->notify       = bluez_device_org_bluez_proximity_reporter1_skeleton_notify;


  bluez_device_org_bluez_proximity_reporter1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_device_org_bluez_proximity_reporter1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (bluezdeviceOrgBluezProximityReporter1SkeletonPrivate));
#endif
}

static void
bluez_device_org_bluez_proximity_reporter1_skeleton_iface_init (bluezdeviceOrgBluezProximityReporter1Iface *iface)
{
  iface->get_link_loss_alert_level = bluez_device_org_bluez_proximity_reporter1_skeleton_get_link_loss_alert_level;
  iface->get_immediate_alert_level = bluez_device_org_bluez_proximity_reporter1_skeleton_get_immediate_alert_level;
}

/**
 * bluez_device_org_bluez_proximity_reporter1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-ProximityReporter1.top_of_page">org.bluez.ProximityReporter1</link>.
 *
 * Returns: (transfer full) (type bluezdeviceOrgBluezProximityReporter1Skeleton): The skeleton object.
 */
bluezdeviceOrgBluezProximityReporter1 *
bluez_device_org_bluez_proximity_reporter1_skeleton_new (void)
{
  return BLUEZ_DEVICE_ORG_BLUEZ_PROXIMITY_REPORTER1 (g_object_new (BLUEZ_DEVICE_TYPE_ORG_BLUEZ_PROXIMITY_REPORTER1_SKELETON, NULL));
}

